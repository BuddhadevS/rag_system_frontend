name: rag_system
description: RAG Document Q&A web frontend for the Spring RAG API.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  dio: ^5.8.0+1
  file_picker: ^9.2.1
  google_fonts: ^6.2.1
  intl: ^0.20.2
  provider: ^6.1.5
  shared_preferences: ^2.5.3
  go_router: ^15.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
